<h3>
  <%= link_to "All recipes (#{Recipe.count})", recipes_path %>
  <button>
    <%= link_to "New recipe", new_recipe_path %>
  </button>
</h3>
<ul>
  <% Recipe.all.each do |recipe|%>
    <li>
      <%= link_to recipe.name, recipe_path(recipe) %>
    </li>
  <% end %>
</ul>
